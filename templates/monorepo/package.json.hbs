{
  "name": "{{project}}",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "workspace": [
    {{#each dir}}
      {{#if @last}}
    "{{this}}"
      {{else}}
    "{{this}}",
      {{/if}}
    {{/each}}
  ],
  "author": "{{author}}",
  "license": "ISC"
}
