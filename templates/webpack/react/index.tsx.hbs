import React from 'react';
import App from './App';
import {createRoot} from "react-dom/client";

const mountNode = document.getElementById("app") as HTMLInputElement;
const root = createRoot(mountNode);
root.render(
  <React.StrictMode>
    <App name="Jane" />
  </React.StrictMode>
);
